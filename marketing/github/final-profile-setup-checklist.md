# Final GitHub Profile Setup Checklist

## Already done

- profile repository created: `ztl970/ztl970`
- profile README pushed
- main public repository banner added
- wide and narrow banner assets generated
- repository description aligned with the current project positioning

## Still worth doing manually

### 1. Refresh GitHub token scope

Run:

```bash
gh auth refresh -h github.com -s user
```

Reason:

- current token can manage repositories
- current token cannot update the account-level GitHub bio

### 2. Set GitHub bio

Recommended bio:

`Building public-safe OpenClaw workflow templates across reusable skill publishing and workflow operations.`

### 3. Pin repositories

Recommended first pin:

1. `openclaw-oss-starter`

If a second public repository appears later:

1. `openclaw-oss-starter`
2. the next public repository that best represents a reusable tool or workflow layer

### 4. Optional profile polish

- add a homepage link later if you create a public landing page
- reuse the wide banner for profile README
- use the narrow banner when you need a tighter preview crop
