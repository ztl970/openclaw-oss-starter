# Feishu Setup Notes

Current local OpenClaw state already includes a loaded Feishu/Lark plugin and
configured accounts.

## Confirmed status

- `channels.feishu.enabled`: true
- default account: `main`
- `media` account exists and already has group allowlist entries

## Needed before sending

- choose the account to post from
- choose the target group or chat
- finalize the announcement text

